# File Sharing Bot

A Telegram bot for file sharing with features like encoding, custom font styling, and more.

## Installation

1. Clone this repository.
2. Create a `.env` file and add your `BOT_TOKEN` and `DATABASE_URL`.
3. Install the required dependencies:
   ```bash
   pip install -r requirements.txt
